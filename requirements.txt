# Gold Dashboard V2 — data integration + API
numpy>=1.24
pandas>=2.0
openpyxl>=3.1
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# Optional: Yahoo Finance fallback (scripts/integrate_bloomberg.py --yahoo-fallback)
# yfinance>=0.2

# Dev / tests
pytest>=8.0
